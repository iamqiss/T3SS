apiVersion: v1
kind: Secret
metadata:
  name: t3ss-secrets
  namespace: t3ss
  labels:
    app.kubernetes.io/name: t3ss
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: secrets
type: Opaque
data:
  # Database credentials (base64 encoded)
  DB_PASSWORD: ""  # Will be set via external-secrets operator
  
  # Redis password (base64 encoded)
  REDIS_PASSWORD: ""  # Will be set via external-secrets operator
  
  # Elasticsearch credentials (base64 encoded)
  ELASTICSEARCH_PASSWORD: ""  # Will be set via external-secrets operator
  
  # JWT secret (base64 encoded)
  JWT_SECRET: ""  # Will be set via external-secrets operator
  
  # OAuth credentials (base64 encoded)
  GOOGLE_OAUTH_CLIENT_SECRET: ""  # Will be set via external-secrets operator
  GITHUB_OAUTH_CLIENT_SECRET: ""  # Will be set via external-secrets operator
  
  # Stripe credentials (base64 encoded)
  STRIPE_SECRET_KEY: ""  # Will be set via external-secrets operator
  STRIPE_WEBHOOK_SECRET: ""  # Will be set via external-secrets operator
  
  # AI/ML API keys (base64 encoded)
  OPENAI_API_KEY: ""  # Will be set via external-secrets operator
  HUGGINGFACE_API_KEY: ""  # Will be set via external-secrets operator
  
  # Storage credentials (base64 encoded)
  AWS_ACCESS_KEY_ID: ""  # Will be set via external-secrets operator
  AWS_SECRET_ACCESS_KEY: ""  # Will be set via external-secrets operator
  
  # TLS certificates (base64 encoded)
  TLS_CERT: ""  # Will be set via cert-manager
  TLS_KEY: ""  # Will be set via cert-manager
  
  # etcd credentials (base64 encoded)
  ETCD_USERNAME: ""  # Will be set via external-secrets operator
  ETCD_PASSWORD: ""  # Will be set via external-secrets operator
  
  # Kafka credentials (base64 encoded)
  KAFKA_USERNAME: ""  # Will be set via external-secrets operator
  KAFKA_PASSWORD: ""  # Will be set via external-secrets operator
---
apiVersion: v1
kind: Secret
metadata:
  name: t3ss-registry-secret
  namespace: t3ss
  labels:
    app.kubernetes.io/name: t3ss
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: registry
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: ""  # Will be set via external-secrets operator
---
apiVersion: v1
kind: Secret
metadata:
  name: t3ss-tls-secret
  namespace: t3ss
  labels:
    app.kubernetes.io/name: t3ss
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: tls
type: kubernetes.io/tls
data:
  tls.crt: ""  # Will be set via cert-manager
  tls.key: ""  # Will be set via cert-manager
---
apiVersion: v1
kind: Secret
metadata:
  name: t3ss-oauth-secrets
  namespace: t3ss
  labels:
    app.kubernetes.io/name: t3ss
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: oauth
type: Opaque
data:
  # OAuth state secrets (base64 encoded)
  OAUTH_STATE_SECRET: ""  # Will be set via external-secrets operator
  OAUTH_CSRF_SECRET: ""  # Will be set via external-secrets operator
  
  # Session secrets (base64 encoded)
  SESSION_SECRET: ""  # Will be set via external-secrets operator
  COOKIE_SECRET: ""  # Will be set via external-secrets operator
---
apiVersion: v1
kind: Secret
metadata:
  name: t3ss-monitoring-secrets
  namespace: t3ss
  labels:
    app.kubernetes.io/name: t3ss
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: monitoring
type: Opaque
data:
  # Prometheus credentials (base64 encoded)
  PROMETHEUS_USERNAME: ""  # Will be set via external-secrets operator
  PROMETHEUS_PASSWORD: ""  # Will be set via external-secrets operator
  
  # Grafana credentials (base64 encoded)
  GRAFANA_ADMIN_PASSWORD: ""  # Will be set via external-secrets operator
  
  # AlertManager credentials (base64 encoded)
  ALERTMANAGER_USERNAME: ""  # Will be set via external-secrets operator
  ALERTMANAGER_PASSWORD: ""  # Will be set via external-secrets operator
  
  # Jaeger credentials (base64 encoded)
  JAEGER_USERNAME: ""  # Will be set via external-secrets operator
  JAEGER_PASSWORD: ""  # Will be set via external-secrets operator
---
apiVersion: v1
kind: Secret
metadata:
  name: t3ss-backup-secrets
  namespace: t3ss
  labels:
    app.kubernetes.io/name: t3ss
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: backup
type: Opaque
data:
  # Backup encryption key (base64 encoded)
  BACKUP_ENCRYPTION_KEY: ""  # Will be set via external-secrets operator
  
  # Backup S3 credentials (base64 encoded)
  BACKUP_S3_ACCESS_KEY_ID: ""  # Will be set via external-secrets operator
  BACKUP_S3_SECRET_ACCESS_KEY: ""  # Will be set via external-secrets operator
  
  # Database backup credentials (base64 encoded)
  BACKUP_DB_PASSWORD: ""  # Will be set via external-secrets operator